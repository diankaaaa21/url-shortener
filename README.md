# URL Shortener with Django, Celery, Docker, Pytest

## Features
- Django + REST API + Web
- Celery for background tasks
- Redis + PostgreSQL
- Dockerized
- Pytest + mocks
- Flower for task monitoring

## Run

```bash
docker compose up --build
